this.stop();
control.setspecial(code.misato,true);
if(control.touko)
{
   control.special.setstart();
   specialwait = true;
}
else
{
   this.play();
}
control.setspecial(code.touko,true);
